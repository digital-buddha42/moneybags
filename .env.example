# Copy to .env for local testing (never commit the real file — it's gitignored).
YNAB_TOKEN=
YNAB_BUDGET_ID=
ANTHROPIC_API_KEY=
# Optional, defaults to claude-sonnet-5:
# ANTHROPIC_MODEL=claude-haiku-4-5
